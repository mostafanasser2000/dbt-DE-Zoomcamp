version: 2
macros:
  - name: get_payment_type
    description: >
      This macro recevies a payment_type and returns the corroessponding description
    arguments:
      - name: payment_type
        type: int
        description: >
          payment_type value.
          Must be one of the accepted values, otherwise the macro will return null